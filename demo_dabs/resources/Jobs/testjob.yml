resources:
  jobs:
    demo01:
      name: demo01_${bundle.target}
      tasks:
        - task_key: test01
          notebook_task:
            notebook_path: ../../src/test01.ipynb
            source: WORKSPACE
          environment_key: Default
      queue:
        enabled: true
      environments:
        - environment_key: Default
          spec:
            environment_version: "4"
      performance_target: PERFORMANCE_OPTIMIZED
